{
  "name": "notica-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --recursive run dev",
    "build": "pnpm --recursive run build",
    "db:studio": "pnpm --filter api exec prisma studio",
    "db:migrate": "pnpm --filter api exec prisma migrate dev"
  }
}
